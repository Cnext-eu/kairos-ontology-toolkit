services:
  kairos:
    build:
      context: .
      dockerfile: service/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - service/.env
    volumes:
      # Mount local ontologies for dev mode
      - ./ontologies:/app/ontologies:ro
    restart: unless-stopped

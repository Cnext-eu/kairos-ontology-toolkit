[project]
name = "{repo_name}"
version = "0.1.0"
description = "Kairos ontology hub — {description}"
requires-python = ">=3.12"
dependencies = [
    "kairos-ontology-toolkit>={toolkit_version}",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

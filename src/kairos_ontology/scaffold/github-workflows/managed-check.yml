# Auto-generated by kairos-ontology-toolkit — do not edit.
# This workflow ensures toolkit-managed files (.github/skills, copilot-instructions)
# have not been modified locally. Run `kairos-ontology update` to refresh them.

name: Managed files check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check-managed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install toolkit
        run: pip install kairos-ontology-toolkit

      - name: Verify managed files
        run: kairos-ontology update --check

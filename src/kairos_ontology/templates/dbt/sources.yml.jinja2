version: 2

sources:
  - name: {{ source_name }}
    description: "Bronze source: {{ system_label }}"
    database: {{ database }}
    schema: {{ schema }}
    tables:
{%- for table in tables %}
      - name: {{ table.name }}
        description: "{{ table.label }}"
{%- endfor %}

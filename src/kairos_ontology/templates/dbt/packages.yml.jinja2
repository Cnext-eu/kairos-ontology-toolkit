# Ranges declared in kairos_ontology.core.adapters (#789). The emitter hard-codes call
# sites against these packages' APIs, so the range is part of the toolkit's contract.
packages:
{% for name, (lower, upper) in dbt_packages %}  - package: {{ name }}
    version: ["{{ lower }}", "{{ upper }}"]
{% endfor %}
# 📘 Domain Model Overview — CLDN Ontology Hub

> Business-friendly overview of all domain models, their structure, and relationships
> Generated by Kairos Ontology Toolkit

---

## Executive Summary

| Metric | Value |
|--------|-------|
| Domain Ontologies | {{ domains | length }} |
| Total Classes | {{ total_classes }} |
| Total Data Properties | {{ total_data_properties }} |
| Total Object Properties | {{ total_object_properties }} |
| Reference Models Used | {{ reference_models | length }} |

---

## Cross-Domain Relationship Diagram

```mermaid
erDiagram
{% for domain in domains %}{% for cls in domain.classes %}    {{ cls.name }} {
{% for prop in cls.data_properties %}        {{ prop.range_short }} {{ prop.name }}
{% endfor %}    }
{% endfor %}{% endfor %}
{% for rel in cross_domain_relationships %}    {{ rel.from_class }} }|--|| {{ rel.to_class }} : "{{ rel.property_name }}"
{% endfor %}{% for rel in intra_domain_relationships %}    {{ rel.from_class }} }|--|| {{ rel.to_class }} : "{{ rel.property_name }}"
{% endfor %}```

---

## Reference Model Lineage

| Domain | Extends | Standard |
|--------|---------|----------|
{% for domain in domains -%}
{% if domain.imports -%}
{% for imp in domain.imports -%}
| {{ domain.label }} | `{{ imp.local_name }}` | {{ imp.standard }} |
{% endfor -%}
{% endif -%}
{% endfor %}
---
{% for domain in domains %}

## {{ domain.label }}

> {{ domain.comment }}

**Version:** {{ domain.version }} · **Classes:** {{ domain.classes | length }} · **Properties:** {{ domain.data_properties_count }} data + {{ domain.object_properties_count }} object

### Class Diagram

```mermaid
classDiagram
{% for cls in domain.classes %}    class {{ cls.name }} {
{% for prop in cls.data_properties %}        +{{ prop.range_short }} {{ prop.name }}
{% endfor %}    }
{% endfor %}
{% for cls in domain.classes %}{% if cls.parent %}    {{ cls.parent }} <|-- {{ cls.name }}
{% endif %}{% endfor %}{% for rel in domain.object_properties %}    {{ rel.domain_class }} --> {{ rel.range_class }} : {{ rel.name }}
{% endfor %}```

### Classes

{% for cls in domain.classes %}
#### {{ cls.label }}

> {{ cls.comment }}
{% if cls.parent %}
_Extends: `{{ cls.parent }}`_
{% endif %}

| Property | Type | Description |
|----------|------|-------------|
{% for prop in cls.data_properties -%}
| `{{ prop.name }}` | {{ prop.range_short }} | {{ prop.comment }} |
{% endfor %}
{% endfor %}
---
{% endfor %}

## Glossary

| Term | Domain | Type | Description |
|------|--------|------|-------------|
{% for entry in glossary -%}
| **{{ entry.label }}** | {{ entry.domain }} | {{ entry.type }} | {{ entry.comment }} |
{% endfor %}

---

_Kairos Ontology Toolkit · Domain Model Overview Report_

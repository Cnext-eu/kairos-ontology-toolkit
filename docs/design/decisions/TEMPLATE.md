# DD-NNN: Imperative one-line title

**Status:** Proposed | Accepted | ~~Superseded by [DD-XXX](dd-xxx-slug.md)~~
**Date:** YYYY-MM-DD
**Affects:** the modules, authored inputs, emitted artifacts or commands this changes
**Issue:** #NNN (optional)
**Implementation:** `path/to/module.py`, `docs/design/dd-NNN-companion.md` (optional)

### Context

What forced the decision. State the problem in terms a reader who was not present can
verify — cite the code or the authored input that misbehaves. Name the constraint that
makes the obvious fix wrong, if there is one.

### Decision

What we are doing, in the present tense. One paragraph if it fits.

### Consequences

What this costs and what it forecloses, including the options considered and rejected —
that is the part people come back for. Note any decision this amends or supersedes, and
mirror the supersession in the superseded file and in the index.

<!--
Filename: this whole `# DD-NNN: Title` heading, lowercased, with every character other
than a letter, digit, underscore, space or hyphen removed and spaces replaced by hyphens.
Truncate at a hyphen if it would exceed 100 characters.

Delete this comment, then add the matching row to
`docs/design/toolkit-design-decisions.md`.
-->

# Power BI Best Practice Analyzer profile

<!-- Generated by `python scripts/generate_bpa_profile.py`. Do not edit by hand. -->

Kairos judges every emitted semantic model against Microsoft's Best Practice Analyzer rules through this curated profile (DD-238). Each rule has exactly one disposition per target:

| Disposition | Meaning |
|---|---|
| `by-construction` | The emitter cannot produce a violation. |
| `compile-diagnostic` | `compile --check` reports it; **blocking** ones fail the plan. |
| `render-assert` | `emit-gold` and `package-powerbi-release` refuse to emit a model that breaks it. |
| `post-deploy-advisory` | Reported by the dataplatform's advisory Semantic Link Labs run after deploy; never blocks. |
| `not-applicable:<target>` | Cannot fire on that storage mode. |
| `rejected:<DD>` | Contradicts a recorded decision. |

Targets: **fabric** is Direct Lake over OneLake; **databricks** is DirectQuery over `Databricks.Catalogs`, published to a Power BI workspace.

An exception is authored in the Gold extension on the `owl:Ontology` resource, and is emitted as the `BestPracticeAnalyzer_IgnoreRules` annotation that Tabular Editor and Semantic Link Labs honour:

```turtle
<ontology> kairos-ext:bpaIgnoreRule "DAX_COLUMNS_FULLY_QUALIFIED on measure sales.margin: reason the rule does not hold here" .
```

## Snapshot

- Upstream: `microsoft/Analysis-Services` `BestPracticeRules/BPARules.json`
- Commit: `50e8ce5028dd7046e73974ec69cb79af225e41b0` (2026-01-21)
- Last refreshed and triaged: 2026-09-24
- Profile version: 1
- Rules: 71 upstream, 3 Kairos-owned

The snapshot is refreshed by hand; see the `kairos-toolkit-ops` release checklist.

## Rules

| Rule | Severity | fabric | databricks | Enforced by | Reason |
|---|---|---|---|---|---|
| `AVOID_FLOATING_POINT_DATA_TYPES` | 2 | compile-diagnostic | compile-diagnostic | `gold.float-column` | A Gold column of canonical type float64 renders as Double. Warned at compile time: the fix is the ontology or Silver type, which the author owns. |
| `ISAVAILABLEINMDX_FALSE_NONATTRIBUTE_COLUMNS` | 2 | post-deploy-advisory | post-deploy-advisory |  | Hidden columns keep IsAvailableInMdx at its default. Turning it off saves attribute-hierarchy memory but changes every hub's TMDL, so it is reported rather than rendered until a decision records the model change. |
| `AVOID_BI-DIRECTIONAL_RELATIONSHIPS_AGAINST_HIGH-CARDINALITY_COLUMNS` | 2 | post-deploy-advisory | post-deploy-advisory |  | Needs VertiPaq statistics that only exist once the model is deployed and refreshed, so it runs in the dataplatform's advisory Semantic Link Labs step. |
| `REDUCE_USAGE_OF_LONG-LENGTH_COLUMNS_WITH_HIGH_CARDINALITY` | 2 | post-deploy-advisory | post-deploy-advisory |  | Needs VertiPaq statistics that only exist once the model is deployed and refreshed, so it runs in the dataplatform's advisory Semantic Link Labs step. |
| `SPLIT_DATE_AND_TIME` | 2 | post-deploy-advisory | post-deploy-advisory |  | Needs VertiPaq statistics that only exist once the model is deployed and refreshed, so it runs in the dataplatform's advisory Semantic Link Labs step. |
| `LARGE_TABLES_SHOULD_BE_PARTITIONED` | 2 | not-applicable:fabric | not-applicable:databricks |  | fabric: A Direct Lake partition is one entity partition over a Delta table; the table is partitioned in the lakehouse, not in the model. databricks: A DirectQuery partition holds no data, so model partitioning has nothing to split; the warehouse table is partitioned in Databricks. |
| `REDUCE_USAGE_OF_CALCULATED_COLUMNS_THAT_USE_THE_RELATED_FUNCTION` | 2 | by-construction | by-construction |  | The emitter never writes calculated columns or calculated tables; Direct Lake does not support them and every Gold column is a Silver column. |
| `SNOWFLAKE_SCHEMA_ARCHITECTURE` | 2 | rejected:DD-238 | rejected:DD-238 |  | Whether a dimension references another is decided by the canonical ontology, not by the renderer. Flattening is authored (product scope, goldExcludeColumn) and surplus filter paths are already deactivated by DD-226. |
| `MODEL_SHOULD_HAVE_A_DATE_TABLE` | 2 | render-assert (blocking) | render-assert (blocking) | `gold.date-table-not-marked` | Every product with an approved calendar emits dim_date marked as a date table with its DateTime key; the assertion checks both on the rendered table. A product with no date role has no calendar to emit. |
| `DATE/CALENDAR_TABLES_SHOULD_BE_MARKED_AS_A_DATE_TABLE` | 2 | render-assert (blocking) | render-assert (blocking) | `gold.date-table-not-marked` | Asserted on the generated dim_date. The rule also matches any table whose name merely contains DATE (e.g. an 'update' log); such a table is not a calendar and takes an authored bpaIgnoreRule. |
| `REMOVE_AUTO-DATE_TABLE` | 2 | by-construction | by-construction |  | Auto date tables are calculated tables, which neither Direct Lake nor DirectQuery models create, and the emitter never writes one. |
| `AVOID_EXCESSIVE_BI-DIRECTIONAL_OR_MANY-TO-MANY_RELATIONSHIPS` | 2 | post-deploy-advisory | post-deploy-advisory |  | A model-wide ratio. Every bidirectional edge the emitter writes is a recorded DD-238 bridge decision, so a small model with one bridge can cross the 30% line legitimately; reported for review rather than gated. |
| `LIMIT_ROW_LEVEL_SECURITY_(RLS)_LOGIC` | 2 | by-construction | by-construction |  | Emitted RLS filters are the fail-closed FALSE() placeholder; the entitlement logic is bound in the workspace, not string-built in TMDL. |
| `MODEL_USING_DIRECT_QUERY_AND_NO_AGGREGATIONS` | 1 | not-applicable:fabric | post-deploy-advisory |  | fabric: Direct Lake tables are not DirectQuery tables. databricks: Every Databricks table is DirectQuery and the emitter authors no aggregation tables. Whether one is worth adding depends on query volume, which only the deployed model shows. |
| `MINIMIZE_POWER_QUERY_TRANSFORMATIONS` | 2 | not-applicable:fabric | by-construction |  | fabric: Direct Lake partitions are entity partitions, not M queries. databricks: The only M the emitter writes navigates Databricks.Catalogs to one table; every transformation lives in dbt. |
| `AVOID_USING_MANY-TO-MANY_RELATIONSHIPS_ON_TABLES_USED_FOR_DYNAMIC_ROW_LEVEL_SECURITY` | 3 | by-construction | by-construction |  | The emitter writes no many-to-many relationship: a bridge is two many-to-one edges (DD-238). |
| `UNPIVOT_PIVOTED_(MONTH)_DATA` | 2 | rejected:DD-238 | rejected:DD-238 |  | Column names come from ontology properties, where a pivoted month layout is a design defect caught in review. The heuristic also matches any name holding 'jan', 'feb' and 'mar' substrings, such as a 'market' column. |
| `MANY-TO-MANY_RELATIONSHIPS_SHOULD_BE_SINGLE-DIRECTION` | 2 | by-construction | by-construction |  | No emitted relationship is many-to-many; the bidirectional bridge edge is many-to-one (DD-238). |
| `REDUCE_USAGE_OF_CALCULATED_TABLES` | 2 | by-construction | by-construction |  | The emitter never writes calculated columns or calculated tables; Direct Lake does not support them and every Gold column is a Silver column. |
| `REMOVE_REDUNDANT_COLUMNS_IN_RELATED_TABLES` | 2 | rejected:DD-221 | rejected:DD-221 |  | Silver lineage and audit columns (_source_system, _loaded_at, ...) are carried on every table by design and hidden by DD-221. Removing one from Gold is goldExcludeColumn's job, decided per product. |
| `MEASURES_USING_TIME_INTELLIGENCE_AND_MODEL_IS_USING_DIRECT_QUERY` | 2 | not-applicable:fabric | post-deploy-advisory |  | fabric: Direct Lake tables are not DirectQuery tables. databricks: The emitted Time Intelligence calculation group uses DATESYTD/QTD/MTD, which cost a warehouse round trip per query in DirectQuery. Whether that is acceptable depends on query volume, so it is reported, not gated. |
| `REDUCE_NUMBER_OF_CALCULATED_COLUMNS` | 2 | by-construction | by-construction |  | The emitter never writes calculated columns or calculated tables; Direct Lake does not support them and every Gold column is a Silver column. |
| `CHECK_IF_BI-DIRECTIONAL_AND_MANY-TO-MANY_RELATIONSHIPS_ARE_VALID` | 1 | by-construction | by-construction |  | Every bidirectional edge is a DD-238 bridge decision -- the default or an authored goldRelationshipCrossFilter -- and carries the ignore annotation recording that it was checked. |
| `CHECK_IF_DYNAMIC_ROW_LEVEL_SECURITY_(RLS)_IS_NECESSARY` | 1 | by-construction | by-construction |  | Emitted RLS filters are FALSE(); none calls USERNAME or USERPRINCIPALNAME. |
| `DAX_COLUMNS_FULLY_QUALIFIED` | 3 | compile-diagnostic (blocking) | compile-diagnostic (blocking) | `gold.dax-column-unqualified` | An unqualified column reference in a measure resolves against the home table only by accident of placement. Blocking: the check is exact for bracketed names the measure declares as column dependencies. |
| `DAX_MEASURES_UNQUALIFIED` | 3 | compile-diagnostic (blocking) | compile-diagnostic (blocking) | `gold.dax-measure-qualified` | A table-qualified measure reference breaks when the measure moves home table. Blocking: exact for references to declared measure dependencies. |
| `AVOID_DUPLICATE_MEASURES` | 2 | post-deploy-advisory | post-deploy-advisory |  | Checkable statically, but not a defect often enough to gate on (DD-163). Reported by the advisory post-deploy run so a reviewer can decide. |
| `USE_THE_TREATAS_FUNCTION_INSTEAD_OF_INTERSECT` | 2 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `USE_THE_DIVIDE_FUNCTION_FOR_DIVISION` | 2 | compile-diagnostic | compile-diagnostic | `gold.dax-division-operator` | Warned, never blocking: '/' is correct wherever the denominator cannot be zero, and the upstream regex cannot tell. |
| `AVOID_USING_THE_IFERROR_FUNCTION` | 2 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `MEASURES_SHOULD_NOT_BE_DIRECT_REFERENCES_OF_OTHER_MEASURES` | 2 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `FILTER_COLUMN_VALUES` | 2 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `FILTER_MEASURE_VALUES_BY_COLUMNS` | 2 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `INACTIVE_RELATIONSHIPS_THAT_ARE_NEVER_ACTIVATED` | 2 | rejected:DD-226 | rejected:DD-226 |  | Surplus filter paths are deactivated deliberately and kept for USERELATIONSHIP; every one is listed in gold_product_report under deactivated_relationships. |
| `AVOID_USING_'1-(X/Y)'_SYNTAX` | 2 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `EVALUATEANDLOG_SHOULD_NOT_BE_USED_IN_PRODUCTION_MODELS` | 1 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `DATA_COLUMNS_MUST_HAVE_A_SOURCE_COLUMN` | 3 | by-construction | by-construction | `gold.column-source-missing` | Every emitted data column names its Silver column as sourceColumn; asserted on the rendered TMDL. |
| `EXPRESSION_RELIANT_OBJECTS_MUST_HAVE_AN_EXPRESSION` | 3 | by-construction | by-construction | `gold.measure-expression-missing` | Only measures past lifecycle 'intent' are emitted, and those require an expression (DD-113); asserted on the rendered TMDL. |
| `AVOID_STRUCTURED_DATA_SOURCES_WITH_PROVIDER_PARTITIONS` | 2 | by-construction | by-construction |  | The emitter writes no data source objects and no query partitions. |
| `AVOID_THE_USERELATIONSHIP_FUNCTION_AND_RLS_AGAINST_THE_SAME_TABLE` | 3 | post-deploy-advisory | post-deploy-advisory |  | A property of authored DAX the compiler does not parse. Reported by the advisory post-deploy run; the fix is an edit to the measure in the hub, never in the deployed model (DD-206, DD-224). |
| `RELATIONSHIP_COLUMNS_SAME_DATA_TYPE` | 3 | render-assert (blocking) | render-assert (blocking) | `gold.relationship-type-mismatch` | Direct Lake refuses a relationship between columns of different types, and DirectQuery joins them with an implicit cast. Blocking on every active relationship, exact on the rendered column types. An inactive edge left by an unproven key (#794) is reported by the post-deploy run instead. |
| `AVOID_INVALID_NAME_CHARACTERS` | 3 | by-construction | by-construction | `gold.measure-display-name-invalid` | Table and column names are Silver identifiers; a measure display name with a control character is rejected at compile time. |
| `AVOID_INVALID_DESCRIPTION_CHARACTERS` | 3 | by-construction | by-construction | `gold.description-control-character` | Descriptions pass through the TMDL text escape, which drops control characters; asserted on the rendered TMDL. |
| `SET_ISAVAILABLEINMDX_TO_TRUE_ON_NECESSARY_COLUMNS` | 3 | by-construction | by-construction |  | The emitter never sets IsAvailableInMdx to false. |
| `UNNECESSARY_COLUMNS` | 2 | rejected:DD-221 | rejected:DD-221 |  | Hidden technical columns are kept on purpose: they carry lineage and can be bound by a security role. Removal is goldExcludeColumn's decision. |
| `UNNECESSARY_MEASURES` | 2 | by-construction | by-construction |  | The emitter never hides a measure or a table that holds one. |
| `FIX_REFERENTIAL_INTEGRITY_VIOLATIONS` | 2 | post-deploy-advisory | post-deploy-advisory |  | Needs VertiPaq statistics that only exist once the model is deployed and refreshed, so it runs in the dataplatform's advisory Semantic Link Labs step. |
| `REMOVE_DATA_SOURCES_NOT_REFERENCED_BY_ANY_PARTITIONS` | 1 | by-construction | by-construction |  | The emitter writes no data source objects. |
| `REMOVE_ROLES_WITH_NO_MEMBERS` | 1 | rejected:DD-238 | rejected:DD-238 |  | Role membership is assigned in the workspace, never in source-controlled TMDL, so every emitted role has no members by design. |
| `ENSURE_TABLES_HAVE_RELATIONSHIPS` | 1 | post-deploy-advisory | post-deploy-advisory |  | Checkable statically, but not a defect often enough to gate on (DD-163). Reported by the advisory post-deploy run so a reviewer can decide. |
| `OBJECTS_WITH_NO_DESCRIPTION` | 1 | compile-diagnostic | compile-diagnostic | `gold.description-missing` | Measures always carry measureDefinition. A visible column without an ontology rdfs:comment is warned, because the description is what a report author reads in the field list. |
| `PERSPECTIVES_WITH_NO_OBJECTS` | 1 | by-construction | by-construction |  | A perspective is emitted only from the tables that declare it. |
| `CALCULATION_GROUPS_WITH_NO_CALCULATION_ITEMS` | 2 | render-assert (blocking) | render-assert (blocking) | `gold.calculation-group-ordinals` | The Time Intelligence group always emits its four items; the calculation-group assertions check items, ordinals and partition. |
| `PARTITION_NAME_SHOULD_MATCH_TABLE_NAME_FOR_SINGLE_PARTITION_TABLES` | 1 | by-construction | by-construction |  | Every emitted partition is named after its table. |
| `SPECIAL_CHARS_IN_OBJECT_NAMES` | 2 | by-construction | by-construction | `gold.measure-display-name-invalid` | Identifiers cannot hold tabs or line breaks, and a measure display name that does is rejected at compile time. |
| `TRIM_OBJECT_NAMES` | 1 | by-construction | by-construction | `gold.measure-display-name-invalid` | Identifiers cannot hold spaces; a measure display name with leading or trailing whitespace is rejected at compile time. |
| `FORMAT_FLAG_COLUMNS_AS_YES/NO_VALUE_STRINGS` | 1 | by-construction | by-construction |  | Flags are Boolean-typed columns, and snake_case names never start with the capitalised 'Is' the rule matches. |
| `OBJECTS_SHOULD_NOT_START_OR_END_WITH_A_SPACE` | 3 | by-construction | by-construction | `gold.measure-display-name-invalid` | Same guarantee as TRIM_OBJECT_NAMES. |
| `DATECOLUMN_FORMATSTRING` | 1 | rejected:DD-238 | rejected:DD-238 |  | Demands the US 'mm/dd/yyyy' format. Date display follows the model culture, which comes from the approved calendar's locale. |
| `MONTHCOLUMN_FORMATSTRING` | 1 | rejected:DD-238 | rejected:DD-238 |  | Demands one fixed English format for any DateTime column whose name contains 'month'; display follows the model culture. |
| `PROVIDE_FORMAT_STRING_FOR_MEASURES` | 3 | by-construction | by-construction | `gold.measure-format-missing` | measureFormatString is mandatory past lifecycle 'intent' (DD-113); asserted on the rendered TMDL. |
| `NUMERIC_COLUMN_SUMMARIZE_BY` | 3 | by-construction | by-construction | `gold.column-summarized` | Every visible emitted column, dim_date's included, carries summarizeBy: none; asserted on the rendered TMDL. |
| `PERCENTAGE_FORMATTING` | 2 | rejected:DD-238 | rejected:DD-238 |  | A house style for percentages. The format string is an authored business decision (measureFormatString), not a defect. |
| `INTEGER_FORMATTING` | 2 | rejected:DD-238 | rejected:DD-238 |  | Despite its name it demands '#,0' or '#,0.0' of every measure that is not currency or a percentage, decimals included. The format is authored. |
| `RELATIONSHIP_COLUMNS_SHOULD_BE_OF_INTEGER_DATA_TYPE` | 1 | rejected:DD-238 | rejected:DD-238 |  | Surrogate keys are deterministic hashes (DD-133), and role-playing dates join dim_date on the date itself. Type equality, which the engine does require, is asserted by RELATIONSHIP_COLUMNS_SAME_DATA_TYPE. |
| `ADD_DATA_CATEGORY_FOR_COLUMNS` | 1 | post-deploy-advisory | post-deploy-advisory |  | Checkable statically, but not a defect often enough to gate on (DD-163). Reported by the advisory post-deploy run so a reviewer can decide. |
| `HIDE_FOREIGN_KEYS` | 2 | post-deploy-advisory | post-deploy-advisory |  | Generated join keys are hidden by DD-221. A role-playing date column stays visible because it is the business date on the row; a product that prefers dim_date hides it with goldHideColumn. |
| `MARK_PRIMARY_KEYS` | 1 | rejected:DD-221 | rejected:DD-221 |  | isKey is emitted only where uniqueness is proven. Power BI rejects a non-unique key at refresh, in the workspace, so an unproven key stays unmarked. |
| `HIDE_FACT_TABLE_COLUMNS` | 2 | post-deploy-advisory | post-deploy-advisory |  | Whether a fact column aggregated by a measure should leave the field list is a product decision; the design skill asks it and goldHideColumn records it. |
| `FIRST_LETTER_OF_OBJECTS_MUST_BE_CAPITALIZED` | 1 | rejected:DD-221 | rejected:DD-221 |  | Tables and columns keep their Silver snake_case identifiers, which every authored DAX expression references. Measures carry an authored display name. |
| `MONTH_(AS_A_STRING)_MUST_BE_SORTED` | 2 | by-construction | by-construction | `gold.calendar-unsorted` | dim_date.month_name sorts by month_number; asserted on the rendered TMDL. A Silver string column whose name merely contains 'month' takes an authored bpaIgnoreRule. |
| `KAIROS_DIRECT_LAKE_GUARDRAILS` | kairos | post-deploy-advisory | not-applicable:databricks |  | fabric: Row and file counts against the capacity's Direct Lake guardrails; sempy_labs.directlake.get_direct_lake_guardrails after deploy. databricks: DirectQuery has no Direct Lake guardrails. |
| `KAIROS_DIRECT_LAKE_FALLBACK` | kairos | post-deploy-advisory | not-applicable:databricks |  | fabric: A Delta source that is a SQL view, or exceeds a guardrail, silently falls back to DirectQuery; sempy_labs.directlake.check_fallback_reason after deploy. databricks: DirectQuery has no fallback. |
| `KAIROS_RELY_ON_REFERENTIAL_INTEGRITY` | kairos | not-applicable:fabric | rejected:DD-238 |  | fabric: relyOnReferentialIntegrity only affects DirectQuery joins. databricks: Assuming referential integrity turns the join into an inner join. A declared unique key proves the one side, not that every many-side key resolves: Silver leaves an unmatched foreign key null, and an inner join would silently drop those fact rows from every total. |

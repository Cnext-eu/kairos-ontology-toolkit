# V5 governed customer hub

This focused fixture proves optional Gold and MDM consumers against the same immutable compile
plan. Its only authored inputs are ontology and source-vocabulary TTL, closed EntityBinding YAML,
and current optional Gold/MDM policy TTL. It contains no generated or retired v4 authority.

All metadata is synthetic and contains no personal data.
